var md5 = require("md5");
var moment = require("moment");
const express = require('express');
const router = express.Router();
var jwt = require("jsonwebtoken");

var sql = require('mssql');
const config = require('../../config');
var debug = require('debug')('http');

var localconfig = {
    server: config.sqlhostname,
    database: config.sqldatabase,
    user: config.sqlUsername,
    password: config.sqlPassword,
    port: config.sqlport,
    encrypt: config.encrypt
};

module.exports = router;

//verify token
router.use(function (req, res, next) {

    // check header or url parameters or post parameters for token
    var token = req.body.token || req.query.token || req.headers['x-access-token'];

    // decode token
    if (token) {

        // verifies secret and checks exp
        jwt.verify(token, "secret", function (err, decoded) {
            if (err) {
                return res.json({ success: false, message: 'Token Expired' });
            } else {
                // if everything is good, save to request for use in other routes
                req.decoded = decoded; next();
            }
        });

    } else {

        // if there is no token
        // return an error
        return res.status(403).send({
            success: false,
            message: 'No token provided.'
        });
    }
});

router.post('/getIndirectIncomes', function (req, res) {

    try {
        var dbConn = new sql.ConnectionPool(localconfig);
        dbConn.connect().then(function () {
            var request = new sql.Request(dbConn);
            request.execute("IndirectIncome_selectAll").then(function (results) {
                console.log(results);

                if (results.recordset === [] || results.recordset.length === 0 || results.recordset === undefined
                    || results.recordset === null) {
                    res.send({
                        success: false,
                        message: "IndirectIncome details not found"
                    });
                } else {
                    res.send({
                        success: true,
                        message: "success",
                        data: results.recordsets
                    });
                }
                dbConn.close();
            }).catch(function (err) {
                dbConn.close();
                console.log(err);
                res.send({
                    success: false,
                    message: err.message
                });
            });
        }).catch(function (err) {
            console.log(err);
            res.send({
                success: false,
                message: err.message
            });
        });

    } catch (error) {
        console.log(error);
        res.send({
            success: false,
            message: error.message
        });
    }
});

// {
// 	"Id":"1"
// }
router.post('/getIndirectIncome', function (req, res) {
    let Id = req.body.Id;
    try {
        if (Id == "") throw "Id is empty";
        var dbConn = new sql.ConnectionPool(localconfig);
        dbConn.connect().then(function () {
            var request = new sql.Request(dbConn);
            request.input('Id', sql.Int, parseInt(Id, 10));
            request.execute("IndirectIncome_selectById").then(function (results) {
                console.log(results);

                if (results.recordset === [] || results.recordset.length === 0 || results.recordset === undefined
                    || results.recordset === null) {
                    res.send({
                        success: false,
                        message: "IndirectIncome details not found"
                    });
                } else {
                    res.send({
                        success: true,
                        message: "success",
                        data: results.recordsets
                    });
                }
                dbConn.close();
            }).catch(function (err) {
                dbConn.close();
                console.log(err);
                res.send({
                    success: false,
                    message: err.message
                });
            });
        }).catch(function (err) {
            console.log(err);
            res.send({
                success: false,
                message: err.message
            });
        });

    } catch (error) {
        console.log(error);
        res.send({
            success: false,
            message: error.message
        });
    }
});

/*
For new record, send Id as -1 else to update existing record, send Id with existing value.
{               "Id": int,
                "IncomeDate": date,
                "AcademicYearId": int,
                "IncomeAccountId": int,
                "ChartId": int,
                "InstitutionId": int,
                "Description": string,
                "PaymentMethodId": int,
                "Amount": int
    }
*/
router.post('/setIndirectIncome', function (req, res) {
    let Id = req.body.Id;
    let IncomeDate = req.body.IncomeDate;
    let AcademicYearId = req.body.AcademicYearId;
    let IncomeAccountId = req.body.IncomeAccountId;
    let ChartId = req.body.ChartId;
    let InstitutionId = req.body.InstitutionId;
    let Description = req.body.Description;
    let PaymentMethodId = req.body.PaymentMethodId;
    let Amount = req.body.Amount;
    console.log(AcademicYearId);
    try {
        if (Id == "") throw "Id is empty";
        if (IncomeDate == "") throw "IncomeDate is empty";
        if (AcademicYearId == "") throw "AcademicYearId is empty";
        if (IncomeAccountId == "") throw "IncomeAccountId is empty";
        if (ChartId == "") throw "ChartId is empty";
        if (InstitutionId == "") throw "InstitutionId is empty";
        if (Description == "") throw "Description is empty";
        if (PaymentMethodId == "") throw "PaymentMethodId is empty";
        if (Amount == "") throw "Amount is empty";

        var dbConn = new sql.ConnectionPool(localconfig);
        dbConn.connect().then(function () {
            var request = new sql.Request(dbConn);
            request.input('Id', parseInt(Id, 10));
            request.input('IncomeDate', sql.VarChar, IncomeDate);
            request.input('AcademicYearId', parseInt(AcademicYearId, 10));
            request.input('IncomeAccountId', parseInt(IncomeAccountId, 10));
            request.input('ChartId', parseInt(ChartId, 10));
            request.input('InstitutionId', parseInt(InstitutionId, 10));
            request.input('Description', sql.VarChar, Description);
            request.input('PaymentMethodId', parseInt(PaymentMethodId, 10));
            request.input('Amount', parseInt(Amount, 10));

            request.execute("IndirectIncome_CreateOrUpdate").then(function (results) {
                console.log(results);
                console.log(AcademicYearId);

                if (results.returnValue != 0) {
                    res.send({
                        success: false,
                        message: "Error while updating IndirectIncome.",
                        results: results
                    });
                } else {
                    res.send({
                        success: true,
                        message: "success",
                        data: results.recordsets
                    });
                }
                dbConn.close();
            }).catch(function (err) {
                dbConn.close();
                console.log(err);
                res.send({
                    success: false,
                    message: err.message
                });
            });
        }).catch(function (err) {
            res.send({
                success: false,
                message: err.message
            });
        });

    } catch (error) {
        console.log(error);
        res.send({
            success: false,
            message: error
        });
    }
});

/*
For delete, send Id.
{
    "Id":"1",
    }
*/

router.post('/delIndirectIncome', function (req, res) {
    let Id = req.body.Id;
    try {
        if (Id == "") throw "Id is empty";

        var dbConn = new sql.ConnectionPool(localconfig);
        dbConn.connect().then(function () {
            var request = new sql.Request(dbConn);
            request.input('Id', parseInt(Id, 10));

            request.execute("IndirectIncome_delete").then(function (results) {
                console.log(results);

                if (results.returnValue != 0) {
                    res.send({
                        success: false,
                        message: "Error while deleting IndirectIncome.",
                        results: results
                    });
                } else {
                    res.send({
                        success: true,
                        message: "success",
                        data: results.recordsets
                    });
                }
                dbConn.close();
            }).catch(function (err) {
                dbConn.close();
                console.log(err);
                res.send({
                    success: false,
                    message: err.message
                });
            });
        }).catch(function (err) {
            res.send({
                success: false,
                message: err.message
            });
        });

    } catch (error) {
        console.log(error);
        res.send({
            success: false,
            message: error
        });
    }
});